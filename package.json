{
  "name": "chatbot-server",
  "version": "1.0.0",
  "description": "Setup LLM using Ollama\r 1. Download Ollama from https://ollama.com/\r 2. pip install langchain\r 3. pip install -U langchain-ollama\r 4. Run sever.py, this will host LLM on http://localhost:8000/\r 5. Test on main.py, and generate the result on the terminal.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
